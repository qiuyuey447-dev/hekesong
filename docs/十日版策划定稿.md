# 《河可松》十日版策划定稿

**版本**：v1.0（定稿）  
**日期**：2026-08-10  
**状态**：内容大改基线 · 替代原「五周 35 日」对外产品形态  
**关联**：`docs/十日版任务清单.md` · 旧弧线参考 `docs/主线剧情.md`（内部保留，不对外）

---

## 0. 定稿声明

本文件为 **产品内容冻结文档**。实现以本文为准；与旧「35 日 / 五周」冲突时，**以十日版为准**。

| 项 | 定稿 |
|----|------|
| 总天数 | **10**（`FINAL_GAME_DAY = 10`） |
| 产品形态 | **完整可通关游戏**（非 Demo、非序章） |
| 核心卖点 | **个性化小狸 + 叙事体验** |
| 种田定位 | **生活质感背景**，非经营目标 |
| 对外周目说法 | 只说「第几天」，不对玩家说「第几周」 |

---

## 0.5 叙事内核（动骨重构 · 权威设定）

> **本节为最高优先叙事约束。** 与前文任何「治愈/她终于记住你」的旧表述冲突时，以本节为准。

### 0.5.1 互为镜子（双向遗忘）

- 玩家「我」独居守着河边一处偏僻的农场（河可松 =「河边，可以松一口气的地方」），把日子过得很淡——**怕失去，所以不敢拥有、不敢许诺**。
- 一个暮色里，会周期性失忆的狐狸**小狸**走了很远来到田埂求收留。我收留她，起初只当「多个帮手」，刻意不往心里放。
- 「她又把我当陌生人」这一刀先砸中小狸；但更深一层的暗线是——**忘的从来不只是她。**「我」也在这个循环里，轮与轮之间同样会重置、忘光；我全程的「似曾相识」是自己前几轮漏出来的记忆。此真相 **D10 才收网**，此前只以暗线埋（眼熟到心里发紧、「这凉意我好像也尝过」、说不清农场荒了多久）。
- **铁律**：① 不引入第三个人物（无阿嬷 / 病房 / 逝去伙伴）；② 小狸的失忆与「重新学你」必须是**真的**，严禁写成「其实一直记得、只是装忘」（那会摧毁个性化）；③ **剧情搭话须与 LLM 联调统一口径**（见 §4.5.5）：信纸正文以策划表为准，主动开口走 LLM 且带齐 `beat_context`，禁止客户端另写与当日节点矛盾的台词。

### 0.5.2 最后一个轮回

- 小狸**不是第一次**来这片田。多年来她来了又走、忘了又回。
- 玩家所玩的这十天，是她**彻底消散/离开前的「最后一个轮回」**。
- D1 基调不是「从零初遇」，而是**「又一次归来」**——她自己不记得，但田/物件/我心里的「似曾相识」**是真有来由的**。
- D6 的「这片田我好像浇过水」从**错觉**升级为**真的浇过**（前几轮留下的真实痕迹）。
- 母题（本子×2、名字、雨、水壶、反复描粗的「不能弄丢这里」）都要能被读成**「跨越多轮的重复」**，强化「十天里其实过了很多轮回」的纵深。

### 0.5.3 永不痊愈（主题落点）

> **我们都会忘。可我们总能，从一片空白里，重新把对方找回来。**

- 结局**没有治愈**。**谁都好不了**——不只是她，也包括「我」。玩家陪的是彼此的**「最后一程」**。
- True/Happy 不是「治好谁」，而是**在会忘的前提下，仍替彼此留下能重新找回对方的锚 / 好好爱了这一程**（D10 两个本子并排、为下一轮留路）。
- Bad/BadEarly 是「这一轮没能留住 / 亲手斩断了那个、一轮轮把你们重新牵到一起的方向」。

### 0.5.4 母题清单

| 母题 | 承载 | 跨轮读法 |
|------|------|----------|
| 本子 ×2 | 小狸的本子（不能弄丢这里）+ 我偷偷记满了她的本子 | 日期跨越远不止十天；两个本子互文，D10 揭示忘的也有我 |
| 名字 | 给小狸重新取名 / 她重新唤回我的名字 | 每一轮都要重新认一次 |
| 雨 | D1 雨夜收留 → D2 廊下 → 结局闭环 | 雨把「回来的路」一次次洗净 |
| 水壶 | 手记得怎么握，脑子却空白 | 身体比记忆更早认得这里 |
| 「不能弄丢这里」 | 本子上被反复描粗的一行 | 被重写过很多轮的执念 |

---

## 1. 产品定义

### 1.1 一句话

> 你独居在河边一处荒了很久的农场，收留了一只会周期性遗忘的狐狸。十天，是她漂泊多年后的**最后一个轮回**：她记下你、忘了你、又在雾里重新认出你。而当雾散尽你才发现——**忘的从来不只是她，也是你；你们是彼此的锚。谁都好不了，但你们总能从空白里，重新把对方找回来。**

### 1.2 核心体验（仅三件事）

1. **被记住** — 名字、约定、日记、习惯写入存档  
2. **被忘记** — 中段陌生化，制造情感落差（并埋下「忘的也有我」的暗线）  
3. **被认回** — 终章不是「全部想起」，而是她**又一次忘光后仍照本子走回田边**；你笑着第 101 次告诉她你是谁——而她也点破：忘的不只是她，你们一直在互相打捞  

### 1.3 明确不做

- 星露谷式经营扩张（多作物 / 大地图 / 第二角色）  
- 35 日赛季密度  
- LLM 改写主线情节或自写长期记忆  
- 聊天框主动刷剧情（ambient 每日最多 1 条）  

### 1.4 对外 Pitch

> 《河可松》— 十日完整故事。与 AI 伙伴小狸一起种田、聊天、留下约定；她会忘记你，也会在雾里慢慢认出你。多结局。

---

## 2. 结构总览

### 2.1 三幕

```text
第1幕  留下（D1–D3）   相识 · 写入 · 建立「我们」
第2幕  雾起（D4–D7）   遗忘 · 选择 · 重新教 · 回响
第3幕  归来（D8–D10）  本子 · 前夜 · 觉醒终章
```

### 2.2 story_mode 映射

| 天数 | story_mode | 对 LLM / 记忆 |
|------|------------|---------------|
| D1–D3 | `prologue` / 日常 | 写入；可正常聊天 |
| D4–D5 | `stranger` | **关闭 episodic**；禁亲昵与具体回忆 |
| D6–D8 | `leak` | Top-3 citable；节点渗漏 |
| D9–D10 | `awaken` | 终章；可连贯认出 |

### 2.3 日程纪律

- **每天必须有主节点**（可 blocking 过天）  
- **每天至少 1 次个性化写入或读取**  
- **禁止空过日**（无节点、无回调、无选择）  

---

## 3. 十日逐日定稿

| 天 | Beat ID（建议） | 情绪任务 | 固定剧情（锁死） | 个性化（必达） | 玩法 | Blocking |
|----|-----------------|----------|------------------|----------------|------|----------|
| **D1** | `T10_D01` | 登门 | 请求留下；取名弹窗 | 写入 `{player_name}` | 教浇 1～2 块田 | 节点后取名 |
| **D2** | `T10_D02` | 日常锚点 | **雨天廊下**（强制雨） | journal 写入雨/廊下 | 雨天免浇；可聊 | 主节点 |
| **D3** | `T10_D03` | 约定 | 傍晚约定进本子 | **强制 `set_promise`** → `{promise.summary}` | 种/浇 | 主节点 |
| **D4** | `T10_D04` | 陌生化 | 「你是谁？」 | 关 episodic；肌肉习惯保留 | 重新介绍；仍要浇田 | 主节点 |
| **D5** | `T10_D05` | 抉择 | 收留 / 赶走 | 写入结局旗标 | 收留则再教浇 | **必选**；赶走→Bad early |
| **D6** | `T10_D06` | 似曾相识 | 廊下/田埂渗漏 1 条 | 从 anchors/journal 抽真实句 | 可投喂触发礼物 déjà vu | 主节点 |
| **D7** | `T10_D07` | 回响 | 叫名 **或** 复述约定；夜树洞 | `{player_name}` / `{promise}` | 夜：陪坐 / 回屋 | 日+夜选 |
| **D8** | `T10_D08` | 本子 | 发现本子日期乱 | `{notebook_excerpt}`←journal | 轻农场 | 主节点 |
| **D9** | `T10_D09` | 前夜 | 「明天想告诉你一件事」 | 昨日回声 + 节律 hint | 收尾田事 | 主节点 |
| **D10** | `T10_D10` | 觉醒 | 碎片墙 + journal 蒙太奇 + 结局 | **必用**玩家 journal 2～3 句 | **纯叙事日**（可锁种田） | 终章不可跳过 |

### 3.1 旧内容迁移参考（实现用）

| 新日 | 主要迁自 | 备注 |
|------|----------|------|
| D1 | `P_N01` + 取名 | 保留 |
| D2 | `P_N02` | `STORY_RAIN_DAYS` 含 2 |
| D3 | `P_N11` | 节点结束 **必须** `set_promise` |
| D4 | `P_N05` | 压缩版陌生化 |
| D5 | `P_N06p` | Bad early 出口改到 D5/D6 |
| D6 | `*_N02p` / Leakage | 个性化回调日 |
| D7 | `*_N16` + `*_N14n` | 合并叫名+夜选 |
| D8 | `P_N06` / F03 | 本子 |
| D9 | `*_N20c` / `*_N33` | 压缩前夜 |
| D10 | 原 D35 觉醒 | 整段前移 |

旧 W3～W5 大量日程节点：**不对玩家开放**，可删或仅作文案素材库。

---

## 4. 个性化定稿

### 4.1 五条必做槽位

| 槽位 / 数据 | 写入 | 读取 |
|-------------|------|------|
| `{player_name}` | D1 取名 | D4 禁用；D7 / D10 启用 |
| `{promise.summary}` | D3 节点结束强制写入 | D7 复述；碎片；D10 |
| `day_journal` | 每晚入睡 | D6 渗漏；D8 本子；**D10 蒙太奇** |
| `prefs`（节律/作物） | D2～D6 行为 | D6+ 开场 / hint |
| 高 salience 聊天或投喂 | D1～D5 | D6 渗漏 1 条 |

### 4.2 每日个性化 KPI

| 天 | 玩家应感到 |
|----|------------|
| D1 | 她叫得出我起的名字（或知道我要她怎么叫） |
| D2 | 雨天廊下是「我们的」第一段日常 |
| D3 | 我们有一句约定进了本子 |
| D4 | 她真的不认得我了（不是 bug） |
| D5 | 我的选择会改变故事 |
| D6 | 她漏出一句 **只有我档才有** 的旧事 |
| D7 | 她叫回我的名字 / 复述我们的约定 |
| D8 | 本子里有我们日子的痕迹 |
| D9 | 她预告「明天有重要的话」 |
| D10 | 蒙太奇里出现 **我的** 日记句 |

### 4.3 双档验收（个性化交付标准）

- 档 A：多聊天、傍晚上线、投喂零食、认真看节点  
- 档 B：几乎不聊天、只种田速过  

**必须不同：** D7 台词（名字/约定）、D10 蒙太奇至少 2 句 journal。  
**否则个性化未交付。**

### 4.4 AI 边界（不变）

- Plot 管发生什么 · Memory 管记得什么 · Persona/LLM 管怎么说  
- LLM **禁止** `memory_write`  
- D4–D5 stranger：OOC / 叫名 / 具体回忆 → 校验拦截  
- 事实锁：田况 / 天气 / 价格以 `GameState` 为准  

### 4.5 亲密度分支（Phase 1+2 · D2/D3/D6/D7/D8/D9）

> **Phase 1+2 范围**：D2、D3、D6、D7、D8、D9 六日主节点/肋骨按亲密度与行为信号分轨。D1/D4/D5/D10 **不做** L3 日程跳过（策划铁律）。

#### 4.5.1 档位定义（L1 · 亲密度 tier）

由 `GameState.get_affection_tier()` 读取当前 `affection`：

| 档位 ID | 区间 | 语义 | 代码常量 |
|---------|------|------|----------|
| **S0** 远 | 0–24 | 公事公办、晚开口、短句 | `AFFECTION_TIER_COLD` |
| **S1** 近 | 25–54 | 有约定心意、留余地 | `AFFECTION_TIER_MID` |
| **S2** 贴 | 55+ | 更敢打断、更私密时段 | `AFFECTION_TIER_WARM` |

**L2** = 行为信号（聊天天数、夜坐次数、约定是否写入、是否已取名）决定 **profile / chat_track**。  
**L3** = 随机时段邀请（`refresh_daily_schedule`）的 **period 白名单 + 权重 + elapsed 比例**；不跳过 blocking 主节点。

#### 4.5.2 亲密度分支表（Phase 1+2）

| 天 | 脊柱 Beat | 变体 ID | L1 gate | L2 gate | 触发时段（L3） | 层级 | template 键 |
|----|-----------|---------|---------|---------|----------------|------|-------------|
| **D2** | `P_N02` | `P_N02_warm` / `_mid` / `_cold` | S2→warm · S1→mid · S0→cold | — | 晨+傍晚（tier 权重） | L1+L3 | `P_N02_{warm\|mid\|cold}` + `P_N02_b_{warm\|mid\|cold}`；`BE_N02` 同逻辑 |
| **D3** | `P_N11` | `P_N11_warm` / `_mid` / `_cold` | S2→warm · S1→mid · S0→cold | — | **仅傍晚**；tier 权重见 `_period_weights_for_tier` | L1+L3 | `P_N03` + `P_N11_{warm\|mid\|cold}` |
| **D6** | `{route}_N02p` | `{route}_N02p_chat` / `_nochat` | tier 影响 schedule 权重 | `chat_days ≥ 1` → chat；否则 nochat | chat：晨+傍晚；nochat：**仅傍晚**（elapsed 偏晚） | L1+L2+L3 | `_N02p_a` / `_b`；nochat→`_N02p_a_nochat` / `_b_nochat`；chat 追加 `{beat}_chat` 步 |
| **D7** | `{route}_N16` | `{route}_N16_warm` / `_mid` / `_cold` | tier 参与 profile 判定 | warm：S2 **且** `has_player_name_set`；mid：S1 **或** `promise` **或** `companionship_nights≥1` **或** `chat_days≥3`；否则 cold | 晨+傍晚（夜选挂在 N16 内，非独立 `_N14n` beat） | L1+L2 | `route_by_suffix._N16` → `{warm\|mid\|cold}`；夜选复用 `_N14n_sit` / `_leave`（cold 专用句） |
| **D8** | `{route}_N15` | `{route}_N15_warm` / `_mid` / `_cold` | tier 参与 profile 判定 | warm：S2 **且**（`chat_days≥2` **或** `day_journal≥4`）；mid：S1 **或** `chat_days≥1` **或** `day_journal≥2`；否则 cold | 晨+傍晚 | L1+L2+L3 | `_N15` route_by_suffix；warm→`_N15_warm` + 追加 `_N15_journal` 步；cold→`_N15_cold`；mid 走 default |
| **D9** | `{route}_N20c` | `{route}_N20c_warm` / `_mid` / `_cold` | S2→warm · S1→mid · S0→cold | — | warm：傍晚+**夜晚**（night 权重 +0.2）；cold/mid：晨+傍晚 | L1+L3 | `_N20c` / `_N20c_b`；warm/cold 有 tier 键；**mid 走 default** |

`{route}` = 结局路线前缀（`NM_` / `HP_` / `TR_` / `BL_`），由 D5 后 `EndingDirector` 锁定。D7 N16 在十日版为 **blocking 主节点**（`_is_rib_beat` 对 `_N16` 返回 false），不可因低亲密度藏起。

**D3 节点后必达**：`complete_beat("P_N11")` 强制 `set_promise` → `{promise.summary}` 写入 long_term_memory（与 tier 无关）。

**D6 chat 步**：`{beat}_chat` 正文由 `StoryRouteData._render_n02p_chat_line` 从 **昨日 journal**（L1 highlights / summary）抽一句，不另存 LLM 记忆。

**D7 夜选 cold**：choice 引导语改为树洞有风；`_N14n_sit` / `_leave` 走 `route_by_suffix` 的 `cold` 变体（`render_companion_night_line` + `get_n16_profile`）。

#### 4.5.3 Gate 字段设计（对接 `story_beat_director`）

实现入口：`scripts/autoload/story_beat_director.gd`。程序员按名调用：

| 职责 | 函数 | 说明 |
|------|------|------|
| 解析当日变体 | `resolve_beat_variant(beat_id)` | 返回 gate 字典（见下表） |
| LLM 上下文 | `get_beat_context_for_llm(beat_id)` | 注入 companion / story API 的 `beat_context` |
| 邀请语气 | `_variant_invite_hint(beat_id, variant)` | 拼进 `get_invite_goal` |
| 日程刷新 | `refresh_daily_schedule()` | 写 `daily_story_trigger` |
| 到点邀请 | `build_scheduled_invite(beat_id)` | 含 `beat_context` + `invite_goal` |
| D7 profile | `get_n16_profile(beat_id)` | warm / mid / cold，供 `_render_n16_line` |
| D8 profile | `get_n15_profile(beat_id)` | warm / mid / cold，供 `_render_n15_tier_line` |
| D8 journal 行数 | `get_n15_journal_max_lines(beat_id)` | warm/mid=3 · cold=1 |

**`resolve_beat_variant` 返回字段**

| 字段 | 类型 | 来源 | 用途 |
|------|------|------|------|
| `variant_id` | String | beat + 后缀 | 日程 seed、存档标记 |
| `tier` | String | `get_affection_tier()` | S0/S1/S2；schedule 权重 |
| `profile` | String | warm/mid/cold | D2/D3/D7/D8/D9 模板路由 |
| `chat_track` | bool | D6：`chat_days≥1` | D6 轨道 + period 白名单 |
| `night_warm` | bool | D7：`nights≥1` 或 profile=warm | LLM hint（夜戏亲密度） |
| `journal_max_lines` | int | D8：warm=3 · cold=1 · mid=3 | N15 填槽行数（`StorySlotService`） |

**`get_beat_context_for_llm` 下发字段**（`beat_context`）

| 字段 | 说明 |
|------|------|
| `beat_id` / `variant_id` | 当前节点与变体 |
| `affection_tier` | S0/S1/S2 |
| `profile` / `chat_track` / `night_warm` | L2 信号 |
| `node_label` / `emotion` | 来自 `StoryRouteData.get_beat_def` |
| `invite_tone` | `_variant_invite_hint` 中文 brief |
| `invite_goal` | `get_invite_goal(beat_id)` 开口方向 brief |
| `scheduled_period` | 自 `daily_story_trigger.period` |
| `chatted_today` | 当日是否聊过（影响 elapsed 微调） |

**持久化 gate 字段**

| 存储键 | 位置 | 内容 |
|--------|------|------|
| `daily_story_trigger` | `long_term_memory` | `{ game_day, beat_id, variant_id, period, elapsed_target, fired }` |
| `story_nodes_seen` | `long_term_memory` | beat_id **与** variant_id 均 `mark_story_node_seen`（`complete_beat`） |
| `relationship_signals.chat_days` | `long_term_memory` | D6 chat_track 门槛 |
| `ending_flags.companionship_nights` | `long_term_memory` | D7 mid/warm、夜选台词 |
| `promise.summary` | `long_term_memory` | D7 mid 门槛；D3 写入 |

常量索引：`BEAT_VARIANT_GATES`（同文件，Phase 1+2 六日 metadata，与上表一致）。

**D2 脊柱不变**：雨天 gate（`STORY_RAIN_DAYS`）与 blocking 行为不改；仅 `P_N02` / `P_N02_b` 文案与 invite 分轨。`BE_N02` 走同一 `resolve_beat_variant` 分支。

**D8 warm 步**：`complete_beat` 前 `build_beat` 在 warm profile 下于 fragment `F03` 前插入 `_N15_journal` 步；`journal_line_1/2/3` 由 `StorySlotService.build_context({ journal_max_lines })` 控制。

#### 4.5.5 LLM 联调铁律（剧情搭话统一口径）

> **违反即 OOC 或口径分裂。** 信纸 / 策划表是事实；LLM 只负责「怎么说」，不得改写节点结论或自造长期记忆。

**须走 LLM 的 event**（`StoryBeatDirector.STORY_LLM_SPEECH_EVENTS`）：

| event | 场景 | 客户端入口 |
|-------|------|------------|
| `companion_proactive` | 到点邀请、渗漏、主动找来 | `main_ui._request_proactive_speech` → `CompanionDirector.collect_llm_extra` |
| `companion_casual` / `morning_sidewrite` | 时段闲聊（同 proactive 管线） | 同上 |
| `player_chat` | 玩家发言 | `main_ui._send_chat_async` |
| `session_start` | 新日/回归问候 | `NpcBridge.request_event` |
| `story_beat` | 信纸节点刚结束 | `NpcBridge.request_event`（预留） |
| `companion_react` | 里程碑/世界事件顺口搭话 | `NpcBridge.request_event` |

**例外（可不 LLM）**：追跑短句（`proactive_chase_line`）、纯 UI toast、信纸 panel 内策划正文。

**payload 必带**（由 `NpcBridge._build_payload` + `get_beat_context_for_llm` 注入）：

- `story_context`：`weekly_hint` / `story_mode` / `pending_beat_*` / `narrative_constraints`
- `beat_context`：`beat_id` / `variant_id` / `profile` / `affection_tier` / `invite_tone` / `invite_goal`
- 主动邀请另带：`proactive_intent=invite` + `proactive_goal`

**双端同步**：变体 gate、`_variant_invite_hint`、`get_invite_goal` 或 `_beat_context_line` 有改动时，须同时改 Godot 与两份 `local_llm_server.py`，并部署 Cloud Run 后再 F5 验收。

#### 4.5.4 未分轨日与验收

| 项 | 说明 |
|----|------|
| D1/D4/D5/D10 | **禁止** L3 schedule 跳过 blocking 节点 |
| 双档肉眼验收 | T10-C3～C8：D2 廊下、D8 本子页数须在档 A/B 可感知 |

---

## 5. 玩法定稿

### 5.1 保留

种萝卜、浇水、收获、出售、派活、天气、商店零食投喂、存档。

### 5.2 节奏

| 天 | 玩法重点 |
|----|----------|
| D1–D3 | 短教学 + 建立日常 |
| D4–D5 | 关系任务压过经营 |
| D6–D8 | 轻循环，突出对话与渗漏 |
| D9 | 收尾田事 |
| D10 | **纯叙事**（建议锁种田或隐藏派活入口） |

### 5.3 天气

- **D2 强制雨天**（剧情对齐）  
- 其余日：随机雨（`weather_seed`），约 38%  
- 可保留其它强制雨日仅当剧情需要；默认十日版仅 D2  

---

## 6. 结局定稿

### 6.1 结局一览

| ID | 名称 | 触发倾向 |
|----|------|----------|
| `ending_normal` | 安顿 | 收留 + 正常推进至 D10；苦甜，把彼此当作要反复重新认识的人；枕边并排两个本子 |
| `ending_happy` | 共处 | 收留 + 聊天/陪伴达标；她好不全，但农场成了家；「你忘我提醒你、我忘你提醒我」 |
| `ending_true` | 最后一程 | 收留 + 约定兑现 + 恢复/碎片阈值；D10 收网「忘的也有我」，两个本子并排为下一轮留锚 |
| `ending_bad_early` | 放手 | **D5 赶走** → D6 **短终章**；她鞠躬道歉离开，我亲手斩断了那个把你们牵到一起的方向 |
| `ending_bad` | 雾中 | 收留但低互动 / 低恢复；没接住她、也没替你们留下那个锚，她像随时飘走 |

> **重要**：True/Happy **不是治愈**。永不痊愈是脊——**谁都好不了，包括「我」**。收束语落在「双向遗忘·互相打捞·为下一轮留锚」上，不写「她终于痊愈 / 永远记得你了」，也不引入第三个人物。

### 6.2 D10 觉醒（非全部想起）

- 触发日：**game_day == 10**（原 35 改为 10）  
- 开场：她**又忘光了今天**，照着本子重新走到田边、重新问「我该怎么称呼你」；这一次「我」**没有崩溃**，笑着第 101 次报出名字。  
- 结构沿用：碎片墙 → journal 蒙太奇 → 结局 VO / F10 变体（标题改为「最后一程 / 这一轮认回了你」）。  
- **D10 收网**：幕三点破「忘的从来不只是她，也是你；你们是彼此的锚」。  
- 结局尾声接**两个本子闭环**：True/Happy/Normal 把她的与我的本子并排、为下一轮留下重新找回彼此的路；Bad/BadEarly 没能留下锚 / 亲手斩断方向。  
- True：最长；Happy：中；Normal：短；Bad：最短或不进完整 F10  

### 6.3 判定输入（压缩到十日可积累）

- `w2_chose_expel`（D5）  
- `companionship_nights`（D7 夜坐）  
- `promise.fulfilled`  
- `affection` / `memory_recovery` / 碎片数  
- 聊天天数 `chat_days`  

具体阈值以实现时 `EndingDirector` 调参为准，**原则：十日能区分四结局，不要求刷满 35 日数值。**

---

## 7. 碎片定稿（减量）

对外 **5 枚**（原 F01–F10 压缩）：

| ID | 标题意向 | 解锁约 |
|----|----------|--------|
| F01 | 登门 / 雨里留下 | D2 或 D6 |
| F02 | 约定 | D3 / D7 |
| F03 | 本子（日期乱） | D8 |
| F04 | 名字 | D7 |
| F05 | 完整的家 | D10 True/Happy |

骨架固定；正文仍走 `StorySlotService.pick_fragment_line` 个性化。

---

## 8. UI / 文案沉浸约定

- Blocking 过天：叙事语气（「她今晚好像有话要说」），禁「未完成任务」式系统腔  
- D4 开场必须 **telegraph**：让玩家知「遗忘是剧情」  
- 标题 / 关于：写「十日完整故事」，不写 Demo / 序章 / 开发中  
- 记忆面板：尽量少暴露 debug 数值；玩家向文案优先「她记得的事」  

---

## 9. 技术落点（文件级）

| 模块 | 改动要点 |
|------|----------|
| `game_state.gd` | `FINAL_GAME_DAY=10`；周逻辑改为十日或弱化「周」；雨日；觉醒日 |
| `story_route_data.gd` | 重写 `DAY_BEATS` 为 D1–D10 |
| `story_beat_director.gd` | prologue 边界、rib/night、D10 终章 |
| `story_director.gd` | story_mode 按上表按日切换 |
| `config/story_nodes.json` | 十日节点精修文案 |
| `ending_director.gd` | D10 觉醒；Bad early 提前 |
| `story_slot_service.gd` | D3 promise 接线确认 |
| `tools/story_test_runner.gd` | 断言改为 10 日 |

**原则：复用引擎，重写日历与文案，不重写 AI 管线。**

---

## 10. 成功标准（完整游戏）

玩家不读文档应能：

1. D1→D10（或 Bad early）**通关并看到结局**  
2. D4 感到遗忘，不以为是 bug  
3. D5 选择有可见后果  
4. D7 或 D10 听到自己的名字或约定  
5. 双档 D10 蒙太奇明显不同  
6. 无 API 可完整通关；有 API 不破坏 stranger / 事实锁  
7. Web / Windows 各至少完整通关一轮无阻断 bug  

---

## 11. 与旧文档关系

| 文档 | 关系 |
|------|------|
| `docs/主线剧情.md` | 世界观 / 个性化规范仍可参考；**天数与日程以本文为准** |
| `docs/策划案.md` | 产品定位以本文 §1 覆盖 |
| `docs/任务进度安排.md` | 执行跟踪见 `docs/十日版任务清单.md` |

---

*定稿确认后开工：先改日历常量与 DAY_BEATS，再填文案，最后通关 QA。*
