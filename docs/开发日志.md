# 《河可松》开发日志

> **使用规则**
> 1. **每次开工前**：读本文件「当前基线」与「下一步」
> 2. **每次收工后**：在「会话记录」追加一条
> 3. **规则类改动**（记忆分层、LLM 职责、防膨胀等）：同步写入「设计规则（备忘）」
> 4. 状态变更同步 `docs/任务进度安排.md`

---

## XL-H 优化方案（2026-08-20 · P1/P2 已落地 · 不做 P3）

对照《休闲小镇》工程纪律。不扩玩法、不加第二 NPC、LLM 不得改主线或自写 `anchors`。  
日终 journal「取舍」**不做**（原 P3 已删）。

### 已经有的（不要重做）

| 块 | 代码事实 |
|----|----------|
| 农事「说了就做」 | `IntentParser` → `ActionExecutor`；`ChoreOrchestrator.execute_reply_followthrough()` |
| 口头承诺补执行 | `main_ui._try_execute_companion_followthrough()` |
| 干活会走过去 | `TaskSystem.start_*` → `CompanionAgent.start_water_job` 等 |
| 主动开口会走近 | `_begin_proactive_approach_for_speech()` |
| 本子有界 | 十日上限 6 页；钉页；写满当面划掉；citable ≤3 |
| 日终摘要 | `DayJournalService` + `day_journal_summarize`；失败保留规则稿 |

### P1 · 历史同构 ✅

- companion 落盘带 `reply_contract`：`{reply, intent, plot_id, actions, cited_memory_ids}`；UI 只显示 `text`。
- `_history_messages()`：玩家纯文本；小狸侧回放 JSON 契约。旧存档无契约时服务端当场包一层。
- 解析失败包成 `{reply, intent:chat, actions:[]}`。双端 `tools/local_llm_server.py` 与 `cloudrun/.../local_llm_server.py`。

### P2 · 话→身体 ✅

每轮最多 1 条 `actions`，与农事 `intent` 分轨。入口：`CompanionBodyAction`。

| id | 落地 | 何时 |
|----|------|------|
| `walk_poi` | `CompanionAgent.go_to_poi` | 玩家让去，或她自己说「我去廊下/树洞/田边…」 |
| `follow_player` | 走近玩家后停下 | 玩家说来，或她说「我过来找你」 |
| `open_notebook` | 打开记忆面板 | 翻本子；`open_memory` intent 已处理则不再叠 |
| `stay` | 不走 | 默认，不写入历史 |

信纸/觉醒/商店/本子面板打开或她正在干活时丢掉动作。廊下台词走位收进同一入口。  
**不做**：送礼、跟钓、八卦网。

---

## 当前基线（2026-08-20 · 晚）

### 项目定位（十日版）

**10 日完整故事**已切日历骨架：`FINAL_GAME_DAY=10`，`IS_TEN_DAY_EDITION=true`。  
**叙事权威**：`docs/世界观设定.md`（**v2 · 模型A：双向遗忘**，撤阿嬷回纯双人）。  
定稿：`docs/十日版策划定稿.md` · 任务：`docs/十日版任务清单.md`。  
交接：`docs/交接.md`（2026-08-20 重写）。

### 本轮已落地

- 日程 / mode / 路线锁日 / HUD / 禁周结算 / D10 觉醒
- D3 promise、D4 telegraph、收获兑现约定、碎片 **F01–F05**（**F10 不算 True**）
- `story_test_runner` 主体可用；**6 个既有失败可忽略**（见交接 §3）
- AI 伙伴 P0～P3（T10-G1～G15）、T10-E 全部
- XL-H P1/P2：历史同构 + 话→身体。**不做** journal 取舍
- 农事「去吧 / 浇」直接执行，不再弹出四选菜单
- D9 划页居中；D6/D10 去掉后台目录腔
- Web 掉帧已压；须重新导出 `build/web`
- 渗透实测：`persona10_infiltrate`（写下 10/10）。08-21 复测口头渗漏 4/10、D6 信纸私页 9/9 留下、D4 假记 0，见 `docs/十日实机_2026-08-21-复测.md`

### 历史定位（2026-07-22，归档）

35 日五周叙事 + 农场经营 + AI 同伴小狸。核心目标：**用结构化长期记忆与分期解锁的节点剧情，建立玩家对 NPC 的长期追求**；AI 负责「怎么说」，Plot 负责「发生什么」。

### 已实现功能清单

#### 玩法与场景（M0～M2）

- Godot 4.7 可运行；星露谷风 TileMap、Y-sort、暖色 UI
- 派活：浇水 / 采集 / 快进；跨天、存档、商店、投喂、大盘
- 亲密度 / 默契 / 心情 / 关系阶段；35 日上限，第五周强制终章

#### AI 与对话（XL-A / XL-B）

- `NpcBridge` + `local_llm_server.py` + `NpcFallback`
- `IntentParser` / `ActionExecutor` / `PersonaGuard`：聊天驱动浇水、越界拒答
- `WorldSnapshot` / `StoryDirector`：世界快照 + 五周 hint
- `RelationshipDirector` + LLM `affection_delta`；`IntentBridge` 可选分类

#### 记忆与个性化（XL-D · 基础）

- `BehaviorCollector`：prefs / persona 五维 / behavior_inferred 漂移
- `GameState.record_memory_event` → anchors（重要性 ≥0.75）
- `MemoryService`：按 story_mode 过滤；W2 stranger 不传 episodic
- `LeakageEngine`：W3+ 锚点渗漏
- `memory_panel`：Debug 对比开关
- **`DayJournalService`（XL-C6）**：日末 journal 双层写入（见下方「设计规则」）

#### 五周叙事与结局（XL-F · 主体）

- `StoryRouteData`：五路线独立 beat_id + 35 日日程
- `StoryBeatDirector`：节点调度、碎片、recovery、路线锁定
- `StoryBeatPanel`：居中半透明节点演出；**支持 choice 步骤**
- **序章 W1 节点文案**（P_N01～P_N06p）按设计稿落地
- **选项并入节点**：P_N06p（D10 收留）、`{路线}_N14n`（D14 夜伴）、N17（D25 夜伴+碎片）
- `EndingDirector` 四结局；`awakening_panel`；`week_wrap_panel`
- `GameState.can_advance_day()`：第 35 天后不可推进
- 节点未完成 blocking：`StoryBeatDirector.has_pending_today_beat()`

#### 已移除 / 收敛的行为

- 聊天框 **主动发言** 已关闭：`story_beat` 后续搭话、`companion_react`、session 渗漏进聊天
- 剧情与选项统一在 **StoryBeatPanel**；`StoryChoicePanel` 无代码引用（场景节点待删）
- 每日固定问候 / 节点内「清晨·小狸想说」由 `StoryBeatDirector._inject_opening_steps` 注入

#### 配置文件

- `config/worldview_setting.json` · `config/xiaoli_persona.json`
- `config/relationship_rules.json` · `docs/api/npc_chat_api.md`

### 已知缺口（对照核心诉求）

| 诉求 | 现状 |
|------|------|
| 长期记忆 | anchors + citable Top-3 + cited 校验；**本子钉页 + 当面划掉**（G6/G7） | ✅ |
| 性格定制 | persona 漂移；**Phase 2 开局定制** ⬜ |
| 人设一致性 | stranger OOC + citation 校验；**人设回归测试 G14** ✅ |
| 记忆膨胀 / 幻觉 | anchor 上限 6（十日）+ 玩家选划 + recency；journal 只存摘要 | 🟡 |
| 话与身体一致 | 农事 Say-Do + `actions` 白名单（过来 / 去 POI / 翻本子） | ✅ |
| 长聊格式稳定 | companion 历史回放 JSON 契约；UI 仍是人话 | ✅ |
| 玩家渗透可见性 | **引用反馈全程 + 本子分页 + 钉页/划掉**（G1/G6/G7）；persona 漂移仍不可见 | 🟡 |

---

## AI 长期记忆方案（Phase 0～3）

> 详见 `docs/主线剧情.md` §10.11 · `docs/任务进度安排.md` XL-C0～C3

**原则**：Plot 管事实 · Memory 管回忆 · Persona 管语气；LLM 不得写入长期层。

| Phase | 内容 | 状态 |
|-------|------|------|
| **0** | Prompt 五段结构 + `citable_memories` + `cited_memory_ids` 校验 | ✅ |
| **1** | 日 journal 规则底座 + LLM 聊天摘要 + anchor 归档 + 节点 slot 全面接线 | 🟡 |
| **2** | 开局软偏好 + bond 档位 + **引用反馈 UI 全程可见** | 🟡 |
| **3** | 周目二记忆继承 + 可选 embedding 检索 | ⬜ |

---

## 设计规则（备忘）

> **约定**：凡涉及玩法边界、记忆分层、LLM 职责划分等**规则类改动**，在本节追加条目，并在「会话记录」留实现索引。

### XL-H · 话落地 / 历史同构

P1/P2 已落地，见文首 **「XL-H 优化方案」**。journal 取舍不做。

---

### XL-G · 本子系统（T10-G6/G7 · 玩家渗透接口）

**目标**：把不可见的 `anchors` 变成可翻、可操作、有代价的物件——「让她记住什么」由玩家决定，「忘是有代价的选择」由玩家参与。

**三层引用纪律（G8 ✅）**

| 层 | 内容 | 能否被 LLM 当作事实引用 |
|----|------|-------------------------|
| L1 工作记忆 | 今日 `today_chat_log` | 可以，仅限当天 |
| L2 情节记忆 | **本子的页**（`anchors`） | **可以，且只有这一层可以** |
| L3 语义记忆 | `persona_vector` / `prefs` | **不可以，只影响语气** |

**钉页**

- 聊天触发词：「记住这个 / 写进本子 / 帮我记住 / 记进本子 / 别忘掉 / 要记得」
- `MemoryService.pin_from_player_chat()`：匹配已有 anchor 或 `record_memory_event` 升格；`pinned=true` + `importance=1.0`
- 驱逐候选池 `_pick_eviction_candidates()` **跳过 pinned**

**写满当面划掉（G7）**

- 原 `enforce_anchor_cap()` 按 `anchor_score` 静默驱逐 → 改为溢出时 `_try_queue_player_eviction()`
- 弹出 `StoryChoicePanel`（场景里一直存在但未接线）：展示分数最低的两条非钉页，玩家选一条划掉
- `resolve_eviction(erase_id)`：真删 anchor + `_merge_evicted_anchors`；之后 LLM 引用不到
- 陌生化期 / 故事结束后仍走静默驱逐兜底

**分数**

- `anchor_score()`：`pinned → 999`；kind 加权；**recency** = `game_day / 当前日 × 0.08`（G15）

**相关文件**

- `scripts/autoload/memory_service.gd` · `scripts/ui/memory_panel.gd` · `scripts/ui/main_ui.gd` · `scripts/ui/story_choice_panel.gd`

### XL-G2 · L3 引用纪律 + persona 回归（T10-G8～G11）

**L3 不可引用（G8）**

- `persona_vector` / `long_term_prefs` / `behavior_inferred` **只调语气**，不得伪装成 L2 情节记忆
- 提示词：`local_llm_server.py` `_prompt_l3_tone_hints()` + `_dialogue_rules` 明写纪律；`dependent` 纳入 `_prompt_persona_tone`
- 校验：`ResponseValidator._violates_l3_episodic_claim()` — 无 `cited_memory_ids` 时出现「上周/那天/我们以前」或把 prefs 说成习惯事实 → `l3_episodic` → NpcFallback

**persona 弹性回归（G9）**

- `GameState.regress_persona_toward_baseline(0.02)` 于 `advance_day()` 每日向 0.5 回归

**behavior_inferred 接线（G10）**

- `memory_service.get_context_for_event()` 下发 `behavior_inferred`

**性格漂移可见化（G11）**

- `peek_persona_shift()`：某维 |value−0.5|≥0.12 且未 announced → 触发 `companion_react` / `persona_shift`
- 台词落点：「我以前好像不这样」；`mark_persona_shift_announced()` 在 react 成功/fallback 后写入
- fallback：`NpcFallback.FALLBACK_REACT_PERSONA_SHIFT` 按 warm/strict/active/optimistic/dependent 分句

**相关文件**

- `game_state.gd` · `memory_service.gd` · `response_validator.gd` · `npc_bridge.gd` · `npc_fallback.gd` · `main_ui.gd` · `local_llm_server.py`（云镜像同步）

### XL-G0 · 主动开口分频道（T10-G1～G5）

**配额**（`game_state.PROACTIVE_CHANNEL_LIMITS`）

| 频道 | 上限 | 时段锁 |
|------|------|--------|
| invite（叙事口） | 2/天 | 无 |
| leak | 1/天 | 无 |
| casual（生活口） | 3/天 | 每时段 1 次 |
| react（反应口） | 2/天 | 每时段 1 次 |
| 全天合计 | ≤5 | — |

**新接线**

- `session_start`：`_maybe_request_session_start()`，D2–D9 每日清晨 LLM 一句（带昨日日志 + 缺席回归）
- `companion_react`：`milestone_trigger` + `crop_became_ready` → LLM，失败 `NpcFallback.companion_react`
- `try_invite(as_remind=true)`：傍晚 `_maybe_remind_story_invite()` 补邀
- **引用反馈**：`_show_citation_feedback()` 全程进聊天（D6–D8 渗漏期去括号混进她的话）

**相关文件**

- `scripts/autoload/game_state.gd` · `scripts/autoload/companion_director.gd` · `scripts/ui/main_ui.gd`

### XL-C6 · 日末 `day_journal`（规则底座 + LLM 聊天摘要）

**目标**：journal 必须反映玩家与小狸的聊天，支撑次日个性化小狸；同时避免记忆膨胀。

**分层职责**

| 层 | 时机 | 来源 | 写入位置 |
|----|------|------|----------|
| L1 规则底座 | 同步（入睡瞬间） | 当日 `short_term_memory` 事件 | `day_journal.summary` / `highlights` / `tags` / `facts` |
| L2 聊天摘要 | 异步（API 返回后 patch） | 当日 `today_chat_log` → LLM `day_journal_summarize` | `day_journal.chat_summary` / `companion_feel` |

**聊天采集**

- `GameState.record_chat_turn()` 双写：`recent_chat_turns`（对话上下文，cap 12）+ `today_chat_log`（日末专用，**cap 16**）
- `advance_day()`：先 snapshot `today_chat_log`，再 `clear_today_chat_log()`；**聊天原文不进入长期层**

**LLM 事件 `day_journal_summarize`**

- 输入：`journal_entry`（规则底座）+ `today_chat_log`（最多 12 轮送 LLM）
- 输出 JSON：`chat_summary`（≤120 字）· `companion_feel`（≤80 字，可空）· `salience`（0~1）
- 结果 patch 到对应 `day` 的 journal；替换规则版「聊天 · …」highlight，避免重复
- **不进聊天 UI**（`main_ui` 对应该事件 early return）

**防膨胀**

| 规则 | 值 / 行为 |
|------|-----------|
| 当日聊天轮次上限 | `MAX_TODAY_CHAT = 16` |
| journal 保留天数 | 7 天（`append_day_journal` 滑动窗口） |
| 长期层内容 | 仅 `chat_summary` 语义摘要，**禁止**聊天原文 |
| `journal_chat` anchor | 仅当 `salience ≥ 0.72` 且 **非 W2 stranger**；**每个 game_day 最多 1 条** |
| 普通 `chat` 事件 | 仍进 `short_term_memory`（cap 12），高 importance 可走通用 anchor 逻辑 |

**Fallback（无 LLM / stranger / API 失败）**

- 规则 `_rule_chat_digest`：取玩家末句或句数统计，写入 `chat_digest_rule` / `chat_summary`
- fallback 时 `salience = 0.55`，**不写** `journal_chat` anchor

**相关文件**

- `scripts/autoload/day_journal_service.gd` · `game_state.gd` · `npc_bridge.gd`
- `tools/local_llm_server.py`（`day_journal_summarize` prompt）
- `scripts/ui/main_ui.gd`（UI 隔离）

### XL-C6b · 本地 fallback 边界 + LLM 剧情上下文

**本地判定（NpcFallback / estimate_local_delta / demo_script）**

- **仅当** `NpcBridge.is_api_enabled() == false`（未配置/未连接 API）时使用
- `demo_script.get_event_override()` **不再**在 API 已连接时短路 LLM（曾导致 week≥3 +「记得」硬编码台词 +「本地判定」提示）
- API 已连接时：HTTP 失败、校验拦截、空回复 → 返回通用短句（`LLM_FAILURE_REPLY`），**不**降级为 `NpcFallback` 脚本台词
- 亲密度：`RelationshipDirector.estimate_local_delta()` 仅在 offline（`used_fallback` 或未接 API）时补算
- `local_llm_server.py --llm` 模式：LLM 调用失败返回 **503**，不再 server 端 mock 降级

**LLM 剧情上下文 `story_context`**

- `StoryDirector.get_story_context_for_llm()`：周目 hint/goal、路线、待触发节点、近期 story_beat、叙事边界
- `NpcBridge` payload 新增 `story_context`；Prompt 首段 `[必达剧情背景 — 回复不得与此矛盾]`
- 闲聊也须遵守 story_mode（修复原先 general 话题隐藏 hint 导致 OOC）

**相关文件**

- `story_director.gd` · `npc_bridge.gd` · `main_ui.gd` · `local_llm_server.py`

### XL-C6c · 玩家称呼（W1 取名 + 遗忘/记起 + LLM 对齐）

**流程**

- W1 D1：`P_N01` 节点结束后弹出取名框，文案「你希望小狸叫你什么」
- `GameState.set_player_display_name()` 写入 `player_name` + `player_name_set`
- 新游戏/重置不再默认「玩家」字面名

**小狸何时知道/能叫名字**

| 阶段 | `companion_knows_name` | `companion_can_say_name` |
|------|------------------------|---------------------------|
| 未取名 | false | false |
| W1 已取名 | true | true |
| W2 陌生化 | false | false |
| W3 渗漏 | false | false |
| W4 D22+ / N16 | true | true |
| W5 觉醒 | true | true |

**LLM**

- payload：`player_name`（仅 knows 时传真名）+ `player_name_context`
- Prompt `[称呼规则]` 段；禁止编造「玩家」为名字
- `ResponseValidator` / `can_use_player_name` 与 `companion_can_say_name` 对齐

**相关文件**

- `game_state.gd` · `name_prompt_panel.gd` · `main_ui.gd` · `memory_service.gd` · `local_llm_server.py`

### XL-C6d · 开始界面「去你的岛」

**入口**

- `project.godot` 主场景改为 `scenes/title_screen.tscn`
- 三按钮：**继续**（读档进主场景）· **新游戏**（删档 + `reset_for_new_game` + 进主场景，有存档时二次确认）· **退出**（`get_tree().quit()`）
- 无存档时「继续」禁用

**存档 API（`GameState`）**

- `has_save_file()` · `delete_save_file()` · `start_new_game_fresh()` · `continue_from_save()`

**视觉**

- 复用 `SuperRetroWorld_Farming_Tileset` / `Autotile_Anim` / 小狸 `fox2_16x20`；木质标牌 + 底部三键；云朵漂移、星星闪烁、标牌轻摆、作物与小狸上下浮动

**相关文件**

- `title_screen.gd` · `title_screen.tscn` · `game_state.gd`

---

## 下一步（十日版）

- [x] T10-M1 · 日历骨架（`FINAL_GAME_DAY=10` / DAY_BEATS / story_mode / 测试全绿）
- [x] T10-M2 · 精修 D1–D10 节点正文与选择文案
- [x] T10-M3 · 双档差异自动化断言（肉眼 F5 仍建议勾一遍）
- [x] T10-G1～G14/G15 · AI 伙伴 P0～P3 全线
- [x] **XL-H P1** · 聊天历史同构回放（assistant 存契约 JSON，UI 仍只显示台词）
- [x] **XL-H P2** · 话→身体：`actions` 白名单（过来 / 去 POI / 翻本子）
- [x] **部署 Cloud Run**（用户 08-21 已重新部署；`/health` = llm + key）
- [x] **再部署 Cloud Run**（闲聊夹渗漏、禁念本子；用户晚间已部署，对照见 `十日实机_2026-08-21-部署后.md`）
- [ ] T10-M5 · F5 肉眼通关：收留 + 赶走
- [ ] T10-C6 · 三档 F5 手测
- [x] T10-D2/D5 · 赶走收束后不再过天；清洗不再抹掉 D5 抉择
- [x] T10-F5 · 标题「十日完整故事」+ 关于弹窗
- [x] T10-F3 · Win 导出（`export/windows/hekesong.exe`，待开包冒烟）

---

### 2026-08-21 | 拦睡觉改 UI 提示

未看完当晚段落时，睡觉不再让小狸开口，只提示「再陪陪小狸」。

---

### 2026-08-21 | 六点实机：重复 / 清晨冷落 / 商店问句 / 投喂本子 / 赖掉 / 信纸引号

**修了**

1. 点空田连说两遍「没种了」：同一句田务反应不再重复进聊天框。
2. 大清早说「怎么一句话没有」：昨天聊过的，早上不再催；问「你咋去商店了」不再当成买种。
3. 投喂记录改成「你喂她红苹果」，不再用系统口吻「你给小狸喂了」。
4. D3 约定「赖掉」改成「忘掉」。
5. 拦睡台词不再说「信纸」；聊天禁止这个词。
6. 聊天剥掉「」/“”和「我抬眼」旁白，避免分不清谁在说话。

双端 `local_llm_server.py` 已同步。**下次**：部署 Cloud Run 后 F5 再对一遍。不要把 F10 算进 True。

---
### 2026-08-21 | 聊天对不上上下文

玩家纠正「你明明在农田」时，小狸回「还有要说的吗，我坐着」——上一句自己说的廊下被丢掉。

**原因**：闲聊场景摘要不带当前位置，模型会编廊下看雨；陌生化还会清空对话历史；校验拦下后本地兜底又是那句坐着等。

**修了**：闲聊也带位置；陌生化保留近几轮（去掉名字）；拦「廊下看雨」类错位；问去哪/纠正位置时兜底改口到真实地点。双端 `local_llm_server.py` 已同步。

**下次**：重新部署 Cloud Run 后 F5 再对一遍「你去哪 → 可是你在农田」。不要把 F10 算进 True。

---
### 2026-08-21 | 部署后再测十人

用户晚间重新部署 Cloud Run。`/health` = llm。十人对照：[`docs/十日实机_2026-08-21-部署后.md`](十日实机_2026-08-21-部署后.md)。平均 4.30；口头 6/10；D4 假记 1/10（I2 仍背约定）。夹渗漏身体句出现。投喂咔嚓偶发。

---

### 2026-08-21 | 对外作品介绍

**做了**

- 提交用文案写到 `docs/作品介绍.md`：简介（短/标准/长）、团队表、AI 玩法、创意点。网页链接 / Demo 视频 / 封面按要求留空。
- 对外名用「去狸的岛 · 十日完整故事」；玩家页不写第十天底牌，评审向单独一节。

**下次**：补团队署名、网页链接、Demo、封面。不要把 F10 算进 True。

---

### 2026-08-21 | 闲聊夹渗漏，禁止念本子

主动口改成闲聊优先，D6–D8 随机把渗漏夹进同一句；LLM/校验/兜底都禁止宣读本子，闲聊不再默认推销种田。双端 `local_llm_server.py` 已同步，**Cloud Run 须再部署**。

---

### 2026-08-21 | 晚间十人实机

`persona10_infiltrate` 再跑。报告：[`docs/十日实机_2026-08-21-晚.md`](十日实机_2026-08-21-晚.md)。平均 4.24。口头 5/10。D4 假记回潮 3/10。云端仍是旧提示词（咔嚓 / 买种）。**须再部署 Cloud Run。**

---

### 2026-08-21 | 复测残留：日记腰 / 连翻 / 投喂抢答

**方案**

- 日记腰扩识别（初次见面 / 你告诉小狸 / 聊起），journal 不再当 `facts.text`；D6 信纸优先玩家原句。
- HTTP 一请求一节点，取消后旧包不能冒充投喂。
- 翻页中的点击记下，动画完再翻；两段短正文并一页。
- 钉页剥「你提到」；「我记下来」不排队；投喂禁咔嚓。双端 LLM 已同步。

**下次**：F5 点信纸/投喂看一眼；**重新部署 Cloud Run** 后投喂口吻才变。不要把 F10 算进 True。

---

### 2026-08-21 | 十人画像全方位实机复测

**做了**

- 重设 `persona10_infiltrate` 口径：口头渗漏与 D6「聊过的字」分列；留下线二者任一算捞回；D4 假记加上背约定/清晨叫名。报告写到 `docs/十日实机_2026-08-21-复测.md`（不覆盖上午那份）。
- Godot 4.7 headless 真挂 `main.tscn` 跑完 I1–I10，墙钟约 11 分钟。平均 **4.53 / 5**。D4 假记 0；留下 D6 空土垄 0；「我记下来」I3/I10 可见。
- 残留：D6 私页仍常抽日记腰（I1/I8/I10）；结局短页连翻；I8 把玩家叫成「田埂」；口头渗漏仍 4/10。

**下次**：F5 点信纸/投喂看一眼；重新部署 Cloud Run 后投喂口吻才变。不要把 F10 算进 True。

---

### 2026-08-21 | 田务开口走大模型

**做了**

- 玩家点田种/浇/收：不再塞「浇了。」「种下了。」这类电报。合并连点后走 `companion_react` / LLM，带回她站在旁边说的 1～2 句；失败才用本地整句兜底。
- 小狸自己浇完/种完/收完：必走 `task_complete` LLM 收工口播。种完再浇的链式活等这句说完再开工，避免被「好，我这就去浇」盖掉。
- 双端 `local_llm_server.py` 与 `farm_plot_reactions.json` 已同步。Cloud Run 需重新部署后线上才是新口径。

**下次**：F5 点一垄、再让她浇完全部，聊天里各应有一句她的话。不要把 F10 算进 True。

---

### 2026-08-21 | T10-D2/D5 通关锁 + 标题关于

**做了**

- 十日存档清洗不再抹掉 D5 去留和 D6 Bad early 的 `story_complete`（旧逻辑按 35 日 D10 抉择写的）。
- 标题加「十日完整故事」副标题和「关于」弹窗。
- 测试：`_test_ten_day_sanitize_keeps_finale`。

**下次**：F5 收留/赶走各走一遍；Win 导出。不要把 F10 算进 True。

---

### 2026-08-20 | 重写交接 + 提交 + Web 导出

**做了**

- 重写 `docs/交接.md`：进度、铁律、静态院子、6 个既有测试失败、F10 不算 True、下一优先改为 T10-M5 / T10-F。
- 同步 `AGENTS.md` 与本文件「当前基线 / 下一步」。
- 提交 Git；本地 `deploy_web.bat` 覆盖 `build/web`（不进仓库）。

**下次**：部署 `xiaoli-api`；T10-M5 肉眼通关。不要把 F10 算进 True。

---

### 2026-08-20 | 去吧不再弹出收浇种菜单

**做了**

- 小狸说「是不是该去浇」时会挂上浇水邀请；玩家回「去吧 / 浇」直接去浇，不再问「收、浇、种还是买种子」。
- 「不用喊两遍」那种听不懂指令的兜底删掉了。

**下次**：不要把 F10 算进 True。

---

### 2026-08-20 | D6 登门页去掉后台摘要

**做了**

- 第六天「登门」碎片不再贴「聊天 · 你们聊了几句……关系仍陌生」这种日结评估，改回田埂来过的正文。
- 日结摘要会剥掉关系档判断；双端 journal 提示不再把「关系阶段」写进给模型看的指令。

**下次**：不要把 F10 算进 True。云上 `xiaoli-api` 未部署则线上日结仍可能写旧腔。

---

### 2026-08-20 | D10 觉醒去掉后台腔

**做了**

- 第十天信纸不再写「登门 —」「失忆物证」「聊天 · 你们聊了 N 句」「归档 ·」「【归来】」这类目录/统计。碎片墙只留剧情句；日记蒙太奇剥掉标签只留发生过的事；「最后一程」改成连贯叙述。

**下次**：不要把 F10 算进 True。

---

### 2026-08-20 | 本子划页弹窗贴左上角

**做了**

- `StoryChoicePanel` 未铺满视口，划页卡缩在左上角、压在篮子底下。现与记忆本一样：全屏锚点 + 视口 `size`、居中卡片、`z_index=80`，打开时提到最前。

**下次**：不要把 F10 算进 True。

---

### 2026-08-20 | 小狸本子去套话、去约定名字栏

**做了**

- 小狸本子不再单独挂「约定」「名字」两栏；名字和约定仍可能作为普通页出现。
- 页上不再写「我怕忘，先写在这里」「我先记下」「我写下来了」「我怕念错，写在第一页」。旧存档打开时也会剥掉。

**下次**：不要把 F10 算进 True。

---

### 2026-08-20 | D4 聊天答不上来

**做了**

- 接口失败不再叠两句（「好。按你的来。」+「没听清」），只回一条正经兜底。
- 「你喜欢吃哪个零食」不再被「喜欢」误判成敷衍答应；陌生化日会答商店零食。
- 失忆日允许「模糊的画面」这类雾词，避免把正经回答拦掉。

**下次**：云上 `xiaoli-api` 未部署则线上仍是旧 mock。不要把 F10 算进 True。

---

### 2026-08-20 | 网页端卡顿掉帧

**做了**

- 网页不再按屏幕 DPR 渲 2× 画布；标题地图半分辨率；雨滴/溅水减量；MCP 运行时只在编辑器连 WebSocket。
- 封面狐狸帧缓存；高亮/本子/树洞不再每帧重画或扫玩家；Web 关 MSAA / 过采样。
- 导出：`gl_compatibility`、关 GDExtension、开 mobile 贴图压缩。须重新 `deploy_web.bat` 才进线上。

**下次**：用户说导出/部署后再测网页。不要把 F10 算进 True。

---

### 2026-08-20 | 闲逛不说话会点一句

**做了**

- 玩家连续不聊天时，清晨开口 / 渗漏 / 闲聊会轻轻点「这几天不怎么开口」。陌生化日只说眼前不吭声，不提「这几天我们」。
- 今天聊过就不再点。不把闲逛改成硬喊私货。

**下次**：复跑 `persona10_infiltrate.tscn` 看 I4。不要把 F10 算进 True。

---

### 2026-08-20 | 十人画像渗透测评文档

**做了**

- 评测：`docs/十人画像渗透测评_2026-08-20.md`（原始逐局仍在 `十人画像渗透实机_2026-08-20.md`）。
- 口径：写下 10/10、渗漏 9/10 可报；4.47 含 D4 采样污染，不对外单报均分。P1 待复跑：叫名、日记腰、引用反馈 0/10、闲逛不叫回、暗线问号落在赶走线。

**下次**：复跑 `persona10_infiltrate.tscn`。不要把 F10 算进 True。

---

### 2026-08-20 | 作品简历

对外一页：`docs/作品简历.md`。点明 **AI NPC × 十日剧情**；中段失忆、第五天去留可剧透；第十天底牌不写。

---

### 2026-08-20 | 渗透实测三处跟进

**做了**

- 聊天等待改采最近一条 **companion** 回句，不再把玩家原句当成她的回答。
- D4–D5：校验用存档名（payload 在陌生化日会把 `player_name` 留空）；清晨开口不再带昨日 journal；双端 LLM 同步。
- 渗漏跳过「你们聊了 N 句」日记腰，兜底不再念「刚才那一下，像真做过：…」。
- 测试：`_test_infiltrate_playtest_followups`

**下次**：F5 或再跑 `persona10_infiltrate.tscn` 看 D4 追问和 D5 清晨还叫不叫名。不要把 F10 算进 True。

---

### 2026-08-20 | 十人画像渗透实测脚本

**做了**

- 新 harness：`tools/persona10_infiltrate.tscn`。十个画像专测「小狸能否记起玩家」，不复用二十画像的通关 QA 权重。
- 相对 `persona20_fullplay`：不掐清晨 `session_start`；D6–D8 白天闲逛最多 18 秒墙钟等口头渗漏（上次 20 局渗漏 0 是过天太快）。
- **已实机跑完** 574s · 10/10 到结局 · 平均 4.47。报告：`docs/十人画像渗透实机_2026-08-20.md`。
- 渗透：写入 10/10；口头渗漏 9/10（赶走对照 I9 为 0，合理）；D6 后叫回 7/10。I10 走到 `ending_true`。
- 残留：引用反馈 0/10；暗线「？」只有赶走局 I9；I4 闲逛渗漏了但没喊出「红围巾」；部分 D4 追问 400ms 只采到玩家原句，D4 真忘证据偏弱。I1/I4 在 D5 陌生化清晨仍叫名/提围巾。I6 渗漏口播了一句 journal 腔。

**下次**：F5 或再跑渗透实测看 D5 还叫不叫名。不要把 F10 算进 True。

---

### 2026-08-20 | 二十画像残留四处

**做了**

- 划掉口播改读 `notebook_line`，不再念 `summary` 系统摘要。
- 「第 N 天，…」日结摘要（含「打理了农场」和「聊天 ·」）不再进她的本子；日结 journal 仍保留给 LLM。
- 当日脊柱信纸优先于划页：`_story_choice_blocked` 只在划页面板真正打开后置位；信纸结束后再弹划页。
- 十日 True 只数 F01–F05；觉醒结束冻结 `locked_ending_id` 再发 F10；D9 `N20c`（非 Bad）发 F05。
- 测试：`_test_ten_day_residual_playtest_fixes`

**下次**：F5 闲逛 D6–D8 看口头渗漏；不要把 F10 算进 True。

---

### 2026-08-20 | XL-H P1/P2 落地（删 P3）

**做了**

- 删掉 journal 取舍（原 P3），不再排期。
- P1：`record_chat_turn` 给 companion 存 `reply_contract`；`_history_messages()` 回放 JSON；解析失败包一层。双端 LLM 同步。
- P2：契约增加 `actions`（每轮最多 1 条）。`CompanionBodyAction` 执行过来 / 去 POI / 翻本子；农事 intent 仍走原链路；信纸/面板/忙碌丢弃。廊下走位收进同一入口。她自己说「我去树洞 / 我过来」也会走；反问「要不要去」不走。
- 测试：`tools/test_xlh_p1p2.py` 全绿；`story_test_runner` 新增 `_test_xlh_history_contract` / `_test_xlh_body_actions` 全绿。

**手测**：F5 说「过来」「一起去廊下」「去树洞」，看她会不会走；聊天记录仍是人话。云 API 需再 `deploy_cloudrun.bat` 后才对线上生效。

**下次**：T10-M4/M5 肉眼通关；不要回头做 journal 取舍，除非用户再提。

---

### 2026-08-20 | XL-H 方案立项（对照代码，未开工实现）

**背景**：看《休闲小镇》宣传页的 ①话变动作 ②历史同构 ③记忆取舍，先核对本仓库再写方案。

**核对结论（不是「什么都没有」）**

- 农事委托、Say-Do 扫词补执行、干活走路、主动开口走近、廊下关键词走位、本子 6 页/钉页/划掉/L3 纪律、journal 摘要防编造约定——**已有**。
- 缺口是：聊天不能结构化驱动「过来 / 去 POI」；历史回放是纯台词；日终 LLM 任务仍是「概括」不是「取舍」。

**产出**

- 本文件「设计规则 → **XL-H**」全文；「下一步」增加 P1/P2/P3 未勾项。
- **未改游戏代码。** 实现时按 P1→P2→P3。

**下次**：若开工，先做 P1 历史同构（`record_chat_turn` + `_history_messages` 双端）。

---

### 2026-08-20 | 长线渗透回路后半段

**完成**

- 本子页改为第一人称 `notebook_line`；节点不再写「白天 · 情绪」废页；十日上限 6 页，每日至多 1 条生活页。
- citable 覆盖开场/反应；十日非陌生化即可引用情节；citation 可从台词推断，已说出口的不再叠系统旁注。
- 种/浇/收短句留在聊天；邀请说过后才允许渗漏闲聊；钉页一次引导 + 本子「留住」；渗漏与「我以前好像不这样」当场在我的本子留问号。
- 测试：`_test_notebook_infiltration_loop`

**下次**：T10-C6 F5 三档；F5 走 D2→D3 看本子人话与清晨回声。

---

### 2026-08-20 | T10-D3 · 觉醒与结局文案切开

**完成**

- 结局面板不再并入 `get_fixed_climax_steps`（碎片墙 / 幕三 / F10 / journal）。觉醒演高潮，结局只播物件落点尾声 + 标题卡 + 字幕。
- 五结局尾声改写进 `story_nodes.json` `endings.epilogue_*`：True 两个本子、Happy 碗与种、Normal 壶与枕边、Bad 空土垄、BadEarly 柜角与泥。不复述幕三对白。
- `story_test_runner` `_test_ending_denouement_split`

**下次**：T10-C6 F5 三档；长线渗透回路 P0。

---

### 2026-08-18 | T10-E1～E6 · Phase E 体验 polish 收尾

**完成**

- **E1**：system 文案键（blocking / sleep / tree hollow）；`main_ui` 统一 `_system_hint()`
- **E2**：D4 telegraph + 首次身份问 toast；LLM session_start 陌生化说明（双端同步）
- **E3/E5/E6 测试**：`_test_ten_day_e_polish()` — 字体、天气、田块、ambient 配额、D4–D5 OOC
- **E6**：`ambient` 频道 ≤1/天；雨天 session 后 `morning_sidewrite` 廊下侧写

**测试**：`story_test_runner` → **ALL PASS**

**下次**：T10-C6 F5 三档；T10-M4/M5 导出。

---

### 2026-08-18 | T10-E4/E7 · fallback 全流程 + 记忆面板去 debug

**完成**

- **fallback 全流程**（E4）
  - `story_test_runner`：事件矩阵（greet/chat/task/react/feed/story_step）+ stranger OOC 禁词断言 + D1–D10 收留/赶走可完成
  - `fallback_smoke_runner` 同步跑三套 fallback 断言
  - `npc_fallback.gd`：D4「你是谁」走失忆回复
- **记忆面板**（E7）
  - 移除调试开关（关记忆/关事实锁/跳终章）
  - 去掉「共 X/12 页」；钉页显示「留住了」

**测试**：`fallback_smoke_runner` + `story_test_runner` → **ALL PASS**

**下次**：T10-E1 blocking 叙事语气；T10-E3 Web 字体/天气。

---

### 2026-08-18 | AI 伙伴 P3 · 「我的本子」+ 暗线问号 + 人设回归测试（T10-G12～G14）

**完成**

- **`PlayerNotebookService` autoload**（`player_notebook_service.gd`）
  - 可见页 / 缺页 / 问号三态；暗线 ID 映射（deja_vu / rain / mutual_forgetting / player_knows_detail / two_notebooks / time_sense_gap / care_direction_slip）
  - `on_beat_completed()` 挂 `story_beat_director.complete_beat()`；D7 依偎 `on_first_write_d7()`；W2 收留 `on_w2_keep_choice()`；D9+ `on_day_advanced()` 缺页
  - D10 觉醒 `reveal_for_awakening()` 逐条亮起问号 → 写入 `ending_director` act2
- **记忆面板第四栏「我的本子」**（`memory_panel.gd`）：标题改「记忆与本子」，宽 1360
- **`StorySlotService._my_notebook_excerpt()`** 优先读玩家本子可见页
- **G14 测试**：`story_test_runner` 加 dark line / D9 缺页 / 觉醒亮起 / L3+stranger+persona 回归断言

**测试**：`story_test_runner` → **=== ALL PASS ===**。

**下次**：T10-C6 F5 三档手测；T10-E fallback 全流程；T10-M4/M5 导出。

---

### 2026-08-18 | AI 伙伴 P2 · L3 纪律 + persona 回归 + 性格漂移开口（T10-G8～G11）

**完成**

- **L3 不可引用纪律**（G8）
  - `ResponseValidator`：`l3_episodic` — 无 citation 时禁「上周/那天/我们以前」及 prefs 习惯事实化（如「总傍晚来」「最喜欢萝卜」）
  - `local_llm_server.py`：`_prompt_l3_tone_hints()` 标注 prefs 为 L3；`_dialogue_rules` 加三层纪律；`npc_bridge` l3 失败走 fallback
- **persona 弹性回归**（G9）：`regress_persona_toward_baseline(0.02)` 挂 `advance_day()`
- **漂移维度接线**（G10）：`behavior_inferred` 进 `memory_context`；`dependent` 进 `_prompt_persona_tone`
- **「我以前好像不这样」**（G11）：`peek_persona_shift()` + `companion_react/persona_shift`；五维各最多开口一次；成功后 `mark_persona_shift_announced`
- 云镜像 `cloudrun/xiaoli-api/tools/local_llm_server.py` 已同步

**测试**：`story_test_runner` + `fallback_smoke_runner` → **=== ALL PASS ===**。

**下次**：T10-G12「我的本子」实体化 + D9 缺页；G13 暗线问号复查。

---

### 2026-08-18 | AI 伙伴 P1 · 本子系统（T10-G6/G7/G15）

**背景**：[`AI伙伴长线设计方案.md`](AI伙伴长线设计方案.md) P1——把 `anchors` 从静默数据结构变成玩家可感知的「她的本子」。

**完成**

- **`MemoryService` 本子核心**（`memory_service.gd`）
  - `get_anchor_pages()` / `pin_anchor_by_id()` / `pin_from_player_chat()` / `looks_like_pin_request()`
  - `enforce_anchor_cap()` 溢出 → `_try_queue_player_eviction()` 发 `anchor_eviction_pending` 信号，不再静默删
  - `resolve_eviction(erase_id)`：玩家选定后真删 + 归档 `week_summaries`
  - `anchor_score()` 加 recency；`pinned` 锚点 score=999，不进划掉候选池
- **记忆面板分页**（`memory_panel.gd`）：「写进去的」显示 `共 X/12 页`、第 N 页、钉标记、天数、摘要
- **划掉仪式 UI**（`main_ui.gd` + 复用 `StoryChoicePanel`）：两页候选 → 玩家选划掉 → 小狸回「……划掉了」
- **聊天钉页**：「写进本子 / 记住这个」等 → 本地确认句，不走 LLM（避免假承诺）
- 存档字段 `long_term_memory.pending_eviction`；读档 `_maybe_resume_pending_eviction()`

**测试**：`story_test_runner` + `fallback_smoke_runner` → **=== ALL PASS ===**；`main.tscn` headless 启动无报错。

**下次**：T10-G8 L3 不可引用纪律（prefs 不得被 LLM 说成具体事件）；G9 persona 弹性回归；G12「我的本子」实体化。

---

### 2026-08-18 | AI 伙伴 P0 · 主动开口密度 + 引用反馈（T10-G1～G5）

**背景**：诊断「像 GALGAME」——采集端完整但回路断在后半段；主动搭话一半写好了没接线。

**完成**

- **引用反馈全程可见**（`main_ui.gd` `_show_citation_feedback`）：聊天引用真实锚点即出「（小狸好像想起了：…）」；渗漏期 D6–D8 去括号直接混进对话
- **主动口分频道配额**（`game_state.gd`）：invite≤2 / leak≤1 / casual≤3 / react≤2，全天≤5；叙事口不受「每时段一次」约束
- **接线 `session_start`**：D2–D9 每日清晨 LLM 活的开场（带昨日日志 + 缺席回归）
- **接线 `companion_react`**：`milestone_trigger` + `crop_became_ready`；夜晚与陌生化期静默
- **接线傍晚二次邀请**：`StoryBeatDirector.try_invite(true)` 于 `_on_time_changed` 傍晚触发
- 方案文档 [`AI伙伴长线设计方案.md`](AI伙伴长线设计方案.md)；任务清单 Phase G；交接文档 §6 同步
- **T10-X1 部分解除**：`companion_react` 已接线但受 react 池约束

**测试**：`story_test_runner` + `fallback_smoke_runner` → **=== ALL PASS ===**。

**下次**：P1 本子系统（见上条）。

---

<!-- 新会话记录追加在本行上方 -->

### 2026-08-12 | 专属渗透 D8 + LLM「未接入」排障（user:// 配置劫持）

**专属渗透（两个本子都带这局私货）**
- 新增槽 `{my_notebook_excerpt}`（`scripts/autoload/story_slot_service.gd`）：解析优先级 ① journal 的 `companion_feel`（对她的观察）→ ② 提到她的日高亮（投喂/喂/陪/聊 或含她名字）→ ③ 相关记忆锚点（gift/chat/journal_chat/companion）→ ④ 兜底句；`_clean_excerpt()` 去「聊天 ·」前缀 + 截断 44 字，并与 `{notebook_excerpt}`（她的本子行）显式去重，避免两本填成同句。
- 接入 D8 `_N15` 的 true/happy/default「我的本子」行（`config/story_nodes.json`）；bad 保留「没写完的破碎感」不注入。文档同步 `docs/十日版主路径正文.md`（D8 三段 + 顶部占位表）。
- 测试：`res://tools/story_test_runner.tscn` → **=== ALL PASS ===**（含 `dual D10 montage differs across saves`）。

**⚠️ 坑：LLM「似乎没接入」的真因 = `user://npc_config.json` 劫持**
- 现象：游戏内小狸回复固定/出现「（本地判定 · 未接大模型）」。
- 根因：`NpcBridge._load_config()` **`user://` 优先于 `res://`**；本机 `%APPDATA%\Godot\app_userdata\hekesong\npc_config.json` 还停在早期本地调试的 `http://127.0.0.1:8080`，而本地服务没跑 → 请求超时失败 → 全程本地兜底。
- 服务端其实正常：Cloud Run `/health` = `{mode: llm, has_api_key: true}`。
- 处置：把该 `user://` 配置 `api_url` 改回线上 Cloud Run（与 `config/npc_config.json` 一致）。
- 备忘：`local_llm_server.py` **只读 `relationship_rules.json`**；persona / worldview / story_nodes 均由**客户端 payload**下发（`res://config` 经 `StoryDirector` / `NpcBridge._persona`），故 `cloudrun/.../worldview_setting.json` 等对运行时不生效——改叙事只需改 `res://config`。**Web 版**的 `user://` 在浏览器 IndexedDB，需清站点存储或以打包进 pck 的 `res://config` 为准。

---

### 2026-08-11 | 二次定调 · 模型A：双向遗忘（撤阿嬷，回纯双人）

**背景**：上一条「互为镜子（阿嬷线）」经讨论后判定**引入了第三个影子**，稀释了「主角只有我和小狸」的原创纯度。进一步推演「忘的其实是我、小狸装忘来救我」这一反转时，发现它会**摧毁个性化小狸**（她必须真学你、不能装忘）。最终定为 **模型A：双向遗忘 · 她的学习为真 · D10 收网**（见 `docs/世界观设定.md` 权威 v2）：
- **纯双人**：撤销阿嬷 / 病房 / 电话回访 / 逝去 furry / 灵魂依附全部设定。
- **她的失忆与「重新学你」是真的**（个性化命根，严禁写成装忘）。
- **暗线**：其实「我」也在循环里、轮间也会忘；全程「似曾相识」是我前几轮漏出的记忆。**仅 D10 收网**，此前只以暗线埋（眼熟到心里发紧、「这凉意我好像也尝过」、说不清农场荒了多久）。
- **永不痊愈**：谁都好不了（含「我」）。**两个本子**（她的「不能弄丢这里」+ 我偷偷记满了她）作核心信物；镜像种子 D2、落笔 D7、揭示 D8。

**改动**
- `config/story_nodes.json`：清阿嬷（P_N01/P_N02/P_N11/P_N05/P_N06p/F01/epilogue_bad_2_body）；`_N15`×4 改「我记满了她」；`_N16` true/normal_warm/default 加 D7 落笔；`act3_true`+`climax_true_2_body` 改双向遗忘收网。
- `scripts/autoload/ending_director.gd`：五结局 epilogue 去电话/病房，改「两个本子并排、为下一轮留锚」（`% player` 计数保持）。
- `config/worldview_setting.json` + `cloudrun/.../worldview_setting.json`：整体换模型A，禁提前点破「玩家也会忘」、永久禁「死去宠物/灵魂」。
- `tools/story_test_runner.gd`：True act3 断言改为「不只是我/你也会忘 + 好不了 + 找回来」。
- 文档：`世界观设定.md` 重写为 v2；`主路径正文.md` 全量重导；`策划定稿.md` §0.5/§1/§6 改模型A。

**测试**：`Godot ... res://tools/story_test_runner.tscn` → **=== ALL PASS ===**（EXIT=0）。

---

### 2026-08-11 | 动骨重构 · 互为镜子 + 永不痊愈 + 最后一个轮回

> ⚠️ 本条的「互为镜子=阿嬷线」与「电话闭环」已被上一条（模型A）取代，仅存档。

**背景**：叙事方向由「基于旧剧情精修」改为**动骨重构**。确立唯一权威内核（见 `docs/十日版策划定稿.md` §0.5）：
- 互为镜子：农场是阿嬷（认知症）的；我逃来这片田，收留会失忆的小狸，赌自己「这回不逃」。「她把我当陌生人」同时砸中小狸与阿嬷（阿嬷线克制埋）。
- 最后一个轮回：小狸多年来来了又走、忘了又回；这十天是她**最后一轮**。D1=又一次归来；D6「似曾相识」从错觉升级为**真的浇过**；母题（本子×2、名字、雨、水壶、「不能弄丢这里」）读作跨轮重复。
- 永不痊愈：结局不治愈。True/Happy=最后一轮被好好记住/告别 + 我终于回去看阿嬷（D10 电话闭环）；Bad/BadEarly=没留住 + 没敢回去看阿嬷。

**全量重写（玩家可见文本）**
- `config/story_nodes.json`：templates（P_N01/P_N02/P_N11/P_N05/P_N06p/BE_N07）、morning `t10_*`、路线 `_N02p_a/_N16/_N15/_N20c`、fragments、awakening（**act3_true 换成永不痊愈核**）、endings、renders、route_shift、choices/system。
- `scripts/autoload/ending_director.gd`：五结局硬编码 epilogue 全改，加入**电话闭环**；D8 `_N15` 加入「我写给阿嬷的本子」互文。
- `scripts/world/demo_script.gd`：D10 session line、D6 leak fallback 强化跨轮。
- 渲染约束遵守：`templates`/`route_by_suffix` ≤1 个 `%s`；避开「仅一个 %s + 这片」的 crop 特例；awakening/endings 走 slot。

**测试**：更新 `story_test_runner.gd` 的 True act3 断言（旧「我记得你了」→「认回来 + 永远记不全/最后一轮」）。跑 `E:\Godot_v4.7-stable_win64.exe --headless res://tools/story_test_runner.tscn` → **=== ALL PASS ===**（含 keep/expel 烟雾、双档差异）。

**文档**：`策划定稿.md` 加 §0.5 内核 + 改结局/§6；`主路径正文.md` 按现行稿重生成（含 D10 觉醒与五结局收束语）。

---

### 2026-08-11 | 手测准备 · 烟雾通关 + 真 bug

**完成**

- 收留 / 赶走 / 双档差异写入 `story_test_runner`，全绿
- 手测清单：`docs/十日版手测清单.md`
- **修**：D7 `N16` 十日版改为脊柱必播；`BE_N07` 不再被当成肋骨 N07；Bad early 尾声 `%` 格式化崩溃

**下次**：按清单 F5 肉眼勾 A/B/C

---

### 2026-08-11 | 十日版节点正文精修（T10-B）

**完成**

- D1–D5 / D6–D9 主路径文案去「第×周」，对齐定稿情绪任务
- D3 约定「进本子」；D5 选项改写；D7 叫名+复述约定；D8 `{notebook_excerpt}`
- 修复 D7 夜选挂在 N16 上无回复台词
- 文案断言写入 `story_test_runner`，全绿

**下次**：双档手测 + 收留/赶走通关

---

### 2026-08-10 | 十日版骨架落地（T10-A + 部分 B/C/D）

**完成**

- `FINAL_GAME_DAY=10`；HUD「第 N/10 天」；禁周结算；debug 跳终章日
- `DAY_BEATS` D1–D9；D4–5 stranger / D6–8 leak / D9–10 awaken
- D3 `P_N11` → `set_promise` + F02；收获兑现约定；D4 telegraph 清晨
- 觉醒/尾声去「第五周」；碎片主线 F01–F05；结局阈值十日版
- `story_test_runner` 全绿（含十日专测）

**下次**：节点正文精修（B2–B10）→ 手测收留/赶走通关

---

### 2026-07-24 | F5 自测 · journal / 周结算

**完成**

- 修复：周结算跨天**前**打开时，第 7 天 journal 用 `DayJournalService.build_entry` 预览补全
- `main_ui`：打开周结算前调用 `prepare_day_end()`
- 新增 headless 自测：`tools/f5_self_test.gd` + `tools/run_f5_self_test.ps1`

**手动验收（Godot F5）**

1. 新游戏 → 浇田/聊天 →「下一天」→ 系统消息出现昨日 summary
2. 某周第 7 天 → 完成节点 → 睡觉 → 周结算「本周回顾」含 **第 7 天** highlights
3. 确认跨周 → 记忆面板可查上周归档

**自动验收**

```powershell
$env:GODOT_EXE = "你的 Godot 4.7.exe 路径"
.\tools\run_f5_self_test.ps1
```

**环境**：本机未检测到 Godot，自动化未执行；脚本已就绪。

**下次**：D35 觉醒演出走读

---

### 2026-07-24 | XL-F10 · W5 节点文案

**完成**

- D29 `*_N19` 连续回忆：登门 / 约定 / 本子三幕蒙太奇 + 四路线收束
- D30 `*_N20a` 稳定叫名 → F07；persona 温/严变体 + `{player_name}` 填槽
- D32 `*_N20b` 写给自己的信 → F08 / F09；BL 仅未完成信
- D33 `*_N33` 宁静日（肋骨，可 deferred）
- D34 `*_N20c` 终章前夕预告
- W5 清晨 d1～d6、节点 followup、搭话 nudge；碎片 F07～F09 fallback
- `story_route_data`：N19 多步、N20a/b 槽位渲染、W5 注入块

**下次**

- D35 觉醒演出走读

---

### 2026-07-24 | XL-F10 · W4 节点文案

**完成**

- D22 `*_N16` 叫名：四路线 + persona 温/严变体，接 `{player_name}`
- D23 `*_N11p` 复述约定 → F04，接 `{promise.summary}`，日期说错情绪对
- D25 `*_N17` 夜树洞 → F05（happy/true 变体；D14 陪坐过走 true_nights）
- D27 `*_N18p` 坚持 → F06（BL 无碎片）
- D28 `*_N08p` 第四周末「好像快想起来了」
- W4 清晨 d1～d7、节点 followup、周结算 hint
- 碎片 F04～F06 fallback 微调

**下次**

- W5 节点（N19 连续回忆起）

---

### 2026-07-24 | XL-F10 · W3 节点文案

**完成**

- D15 `*_N09` 帮工来过：四路线 + 修 N09 填槽（crop/companion）
- D16 `*_N02p` 雨天廊下 → F01；**BL 路线不解锁 F01 碎片**
- D17 `*_N13` 约定半句：独立文案 + `{promise.summary}` 槽
- D18 `*_N14` 收获 → F02；D21 `*_N15` 树洞本子 → F03（四路线周末文案）
- W3 清晨注入（d1～d7）、节点 followup、周结算 hint
- 碎片 F01～F03 fallback 微调

**下次**

- W4 节点（N16 叫名起）

---

### 2026-07-24 | 节点文案集中 · story_nodes.json

**完成**

- `config/story_nodes.json`：W1～W5 节点正文、路线变体、碎片、清晨/搭话/周结算、选项
- autoload `StoryNodeCopy`；`StoryRouteData` 等改从 JSON 读取

**用法**：编辑 JSON → 重启 Godot；别名 `"BE_N01": "@P_N01"`

---

### 2026-07-24 | XL-F10 · W2 节点文案

**完成**

- `P_N05` / `P_N06` / `P_N06p`：陌生化开场、本子、抉择
- D11 N07 投喂「这颜色…等过很久」；D12–13 清晨重新介绍
- D14 夜树洞 `*_N14n` + 周结算 W2 预告
- 移除 W2「欢迎回来」清晨 OOC

**下次**

- W3 节点（N09 起）

---

### 2026-07-24 | Phase 1 · XL-C5 20 轮压测

**完成**

- 新增 `tools/stress_test_npc.py`：20 轮 stranger/normal/task/session 压测，JSON 日志
- mock 基线：**OOC 0/20，幻觉 0/20** PASS（`tools/logs/xl-c5/`）
- 修复 stranger fallback 含「我们以前」误触 OOC 校验

**下次**

- XL-F10 W2～W5 节点文案

---

### 2026-07-24 | Phase 1 · XL-C4 API 文档

**完成**

- `docs/api/npc_chat_api.md` 升级 v2.0：全 event、payload、`memory_context`、校验降级、stranger OOC、投喂、journal、IntentBridge
- `config/npc_config.example.json` / `tools/npc_config.example.json` 与 `NpcBridge` 字段对齐

**下次**

- XL-C5 20 轮压测

---

### 2026-07-23 | Phase 1 · XL-C3 stranger OOC 拦截

**完成**

- `relationship_rules.json`：`stranger_ooc_phrases` / `stranger_intimate_phrases`
- `ResponseValidator`：story 事件统一校验具体回忆、亲昵、玩家名
- `NpcBridge`：校验失败时降级 `NpcFallback`（非 LLM 失败提示）
- `NpcFallback`：W2 stranger 问候/聊天专用 fallback；修复「欢迎回来」泄漏
- `local_llm_server`：`is_stranger_ooc_reply()` + post-LLM 替换为 stranger mock

**下次**

- XL-C5 20 轮压测

---

### 2026-07-23 | Phase 1 · XL-C9 引用反馈

**完成**

- `NpcBridge` 校验通过后缓存 `cited_memory_ids`，`take_cited_memory_ids()` 供 UI 消费
- `MemoryService.build_citation_feedback()`：单条显示摘要，多条显示「连起几件旧事」
- `main_ui` 在 `player_chat` 回复后追加 subtle 系统行（仅 API 成功且非 fallback）

**下次**

- XL-C4 API 文档 · XL-C5 20 轮压测

---

### 2026-07-22 | Phase 1 · XL-C6+ 混合 journal

**规则**（详见「设计规则 → XL-C6」）

- 双层 journal：L1 规则同步 + L2 LLM 聊天摘要异步 patch
- 防膨胀：原文仅当日 buffer；长期只存摘要；`journal_chat` anchor 每日 ≤1 且 salience 门槛

**实现**

- `DayJournalService.request_llm_enrich()` / `apply_enrichment()`；事件 `day_journal_summarize`
- `GameState.today_chat_log` · `patch_day_journal_entry()` · `record_memory_event("journal_chat")` 去重
- `npc_bridge.gd` / `local_llm_server.py` / `main_ui.gd` 接线

**下次**

- XL-C7 anchor 合并

---

### 2026-07-22 | Phase 1 · XL-C6 日 journal

**完成**

- 新增 `DayJournalService`：从当日 `short_term_memory` 按优先级拼 highlights / tags / summary
- `GameState.advance_day()` 改为规则 journal；`record_memory_event` 增加 `game_day` 字段
- `week_wrap_panel` / `EndingDirector` 优先展示 journal highlights

**下次**

- XL-C7 anchor 合并

---

### 2026-07-22 | Phase 0 开工

**计划**

- 清除旧日志，写入已实现基线与 AI 方案
- 同步 `任务进度安排.md`、`主线剧情.md`
- 实现 Phase 0：记忆引用契约 + 校验

**完成**

- 重写 `开发日志.md`：已实现基线 + AI Phase 0～3 方案
- 重写 `任务进度安排.md`：XL-C0～C2 为当前主线；XL-E 收敛；XL-F8/F9 完成
- 更新 `主线剧情.md` §10.9/§10.11、§11 节点/chat 分工
- Phase 0 代码：`MemoryService` citable/validate · `ResponseValidator` citation/stranger · `NpcBridge` · `local_llm_server.py` 五段 Prompt

**验收**

- F5：待用户确认
- cited 校验：已实现（非法 id / W2 stranger → Fallback）

**下次**

- Phase 1 XL-C6：日末 journal 规则写入

---

<!-- 新会话记录追加在本行上方 -->

### 2026-07-23 | Phase 1 · XL-C8 节点 slot 接线

**规则**

- P 节点模板用 `{slot}` 填槽；读不到走 fallback，不改固定剧情事实
- 数据源：`promise` / `prefs` / `day_journal` / `week_summaries` / `anchors`

**实现**

- 新增 `StorySlotService`：`build_context` / `apply` / 碎片行 / 叫名与约定台词
- `StoryRouteData.render_body` + 清晨/搭话/后续步统一过 slot
- N06 本子摘录、N07 投喂、N13/N11p 约定、N16 叫名（persona 分支）接存档

**下次**

- XL-C9 引用命中 subtle 系统反馈

### 2026-07-23 | Phase 1 · XL-C7 anchor 合并

**规则**

- anchor 上限 12；超出时按 salience/importance 淘汰最低条目
- 淘汰 anchor 合并进对应周的 `week_summaries`（`merged_highlights` / `merged_anchor_ids`）
- 跨周时 `day_journal` 归档为周摘要，再清空当日 journal

**实现**

- `MemoryService.enforce_anchor_cap()` · `finalize_week_archive()` · `get_week_summary()`
- `GameState.record_memory_event()` 改调 salience 合并；`_reset_for_new_week()` 归档 journal
- `week_wrap_panel` / `memory_panel` / `EndingDirector` 展示归档摘要


### 2026-07-22 | 开始界面「去你的岛」

**规则**（详见「设计规则 → XL-C6d」）

- 主场景标题页；继续/新游戏/退出三键与存档联动

**实现**

- `title_screen.gd` · `GameState` 存档辅助 API · `project.godot` 主场景切换

---

**规则**（详见「设计规则 → XL-C6c」）

- W1 取名弹窗；W2–W3 忘记；W4 D22 / N16 后可再叫名
- LLM 按 `player_name_context` 决定是否传真名、能否直呼

**实现**

- `NamePromptPanel` · `GameState` 名字状态 API · Prompt/校验对齐

---

### 2026-08-18 | 信任链专项写入体验建议

**产出**

- `docs/十日版玩家体验与改进建议.md` 新增 **§6 信任链**：五段链条、已落地项、P0 T1–T5、P1/P2、禁止做法、验收清单。
- 原 §6–§12 顺延为 §7–§13；§9 P0 #1 对齐 §6.4；发布就绪度增加「信任链 🔴」。

---

### 2026-08-18 | 二十人玩家体验模拟 → 改进建议文档

**产出**

- 新增 `docs/十日版玩家体验与改进建议.md`：两轮共 20 种玩家画像、高频好评/差评、P0–P2 排期、目标用户与发布就绪度。
- 同日晚追加 **§6 交互体验**、**§7 声画与 BGM** 专项（对照 `UI设计稿`、`bgm_director.gd`、`rain_world.gd`）。
- `docs/交接.md` 关键路径表 + §6 未完成项增加对该文档引用；P0 强调 **T10-E2**（D4 防误判）、**T10-E1**（blocking 叙事语气）、**T10-F5**（名称统一）。

**结论摘要**

- 叙事/情感型满意度 ~4.5/5；Web/触屏、纯种田 ~3/5。
- 交互 ROI：话区设计稿落地 + 田务 toast 改口吻；声画 ROI：D2 雨环境音 + D4/D10 stinger。
- 下一小时 ROI：D4 防误判 + blocking 改口 + 对外名称，优于加新功能。

---

### 2026-07-22 | 本地 fallback 边界 + LLM 剧情上下文

**规则**（详见「设计规则 → XL-C6b」）

- 本地判定仅 API 未连接时；已连接则失败走通用短句，不降级 NpcFallback
- LLM 注入 `story_context`（周目/路线/待触发节点/叙事边界），修复闲聊 OOC

**实现**

- `npc_bridge.gd` · `story_director.gd` · `main_ui.gd` · `local_llm_server.py`

---

### 2026-08-21 | 十日实机（渗漏变密之后）

`tools/persona10_infiltrate.tscn` 10 画像跑完。报告：[`docs/十日实机_2026-08-21.md`](十日实机_2026-08-21.md)。

- **成立**：D6「聊过的字」能抽槐花蜜 / 路灯 / 槐秋；I2 D8「写着也记着，一抬头忘了是跟谁说的」；D4 I1/I10 真忘；I9 短终章；I10 True。
- **P0**：留下线 D6 信纸误用 Bad「空土垄」；I2 D4 把约定当事实说；I4 D4 清晨叫「红巾」。
- 口头渗漏表计 3/10（上次 9/10），信纸私页补上了，闲逛漏变少。

**下次**：先修 D6 留下线 route 文案，再修 D4 假记/叫名。

---

### 2026-08-21 | 修实机 P0：空土垄 / D4 假记 / 叫名

1. **留下线 D6 不再锁 Bad「空土垄」**：十日互动靶按 10 日缩放；`nights==0` 的 Bad 门等到 D8（夜坐之后）；D6 留下即使投影仍是 Bad 也落到 Normal，信纸走「手还记得」。
2. **D4 陌生化**：payload 不再带昨日日记/聊天史；校验拦存档名和约定原文；问起约定走「想不起来」。双端 LLM 同步（**需重新部署 Cloud Run**）。

测试：`_test_keep_d6_letter_not_empty_ridge` · `_test_stranger_blocks_name_and_promise`。已知 6 条旧 fail 未动。

---

### 2026-08-21 | 修十人实机 P1/P2

对照 `docs/十日实机_2026-08-21.md`。P0 三项不重做。

1. 渗漏口不再念「你们聊到 / 你问小狸」日记腰；空锚走田埂身体句。
2. D8 `notebook_excerpt` 走玩家可见清洗，不上「聊天 · 你们聊了 N 句」。
3. 留下线 D7 信纸有名就叫名，不再改口「这儿的主人」。
4. 短尾声并页、短页跳过打字机，避免同一页空翻。
5. 投喂前取消进行中的闲聊；旧回包不当胡萝卜句；失败仍用本地 déjà vu。
6. D6「聊过的字」跳过钉页口令与追问句。
7. 钉页回句只套一层引号。
8. 「我记下来」立刻写下上一句私货；`player_choice` 页 D9 不缺页。
9. 信纸不消耗口头渗漏锚。

双端 `local_llm_server.py` 已同步 digest。**Cloud Run 仍需部署。**

测试：`_test_persona10_bugfix_batch`。已知 6 条旧 fail 未动。

---

### 2026-08-21 | 闲聊夹渗漏，禁止念本子

玩家要求：渗漏不要宣读本子；主动开口以闲聊为主，随机夹渗漏；话题不只农田。

1. `CompanionDirector.pick_speech`：D6–D8 先走 casual；按时段随机 `also_leak`。未 mix 时不再把 `leak_context` 塞进闲聊。闲聊名额用尽才单独夹渗漏。
2. 双端 LLM：闲聊话题扩到廊下/树洞/河/壶/灯；渗漏须化成感觉或口误；禁止「本子上写着／翻到那页」。
3. 校验拦宣读本子、拦「买点种子」硬推销。Fallback 不再粘锚点原文。

**Cloud Run 须再部署** 后线上才吃到新提示词。

---

### 2026-08-21 | 渗漏变密：D6 真锚点 / 投喂 déjà vu / 我的本子可写

玩家反馈渗漏太稀。没加新采集字段，只把已有接口接活。

1. **D6 信纸**不再跳过「聊过的字」。有玩家原话 / 日记 chat_summary / 约定时插入 `_N02p_chat` 页；不再用「你说过的话，她本子上有一行」这种假句。口头渗漏优先抽 `chat` / `journal_chat`，再落到约定。
2. **投喂 déjà vu**：`LeakageEngine.try_feed_leak` / `peek_feed_leak_context` 从 `_on_feed_requested` 接入；十日用 D6–D8 而不是 `week>=2`（十日里 D6 仍是第 1 周，旧门永远关着）。`gift_deja_vu` 回到 live `renders`。LLM `companion_feed` 带 `leak_context`（双端已同步，**需重新部署 Cloud Run**）。
3. **我的本子**：聊天说「写进我的本子 / 我记下来：…」会写入可见页；她的本子页多了「写进我的本子」链接。`写进本子` 仍是钉她的页。

测试：`_test_d6_letter_uses_real_chat` · `_test_feed_leak_ten_day` · `_test_player_notebook_write_choice`。

---

### 2026-08-21 | 问句不当令 / 买种没买 / 本子公文腔

实机：①「你不认识我咋还帮我收萝卜」被当成代收；②「我是问你是不是帮我收的」落到「听着呢」；③她说去买种子，玩家「买5包吧」被忙线吞掉，追问「你咋没买」却答「还没有能收的」；④ D8 树洞本子摘录像公文。

1. `IntentParser.looks_like_chore_question`：咋/是不是/我是问/咋没买 走聊天，不走 harvest/shop。邀约确认也跳过问句。双端 LLM 同步。
2. 本地先答：为什么帮忙、是不是你收的、你咋没买。买种数字提到忙碌线前面；忙着也记下包数。
3. 本子摘录拒「简短道别 / 通水沟」一类公文；N15 mid 改成她会说的短句。

**Cloud Run 须再部署** 后线上才吃到新提示词。

---

### 2026-08-21 | 九条实机复测 + 日常闲聊渗透

对照「六点实机」+ 后来四条（听不懂合并进问句不当令）。

客户端九条全过（`_test_nine_bug_retest_and_daily_chat`）。日常句「为什么帮我 / 你怎么了 / 今天太阳挺好的 / 你咋没买酒」不再进代收/代买。真命令「帮我收萝卜 / 浇水 / 去商店买种子」仍在。

Cloud Run `/health` = llm。线上问句 intent=chat，日常「为什么帮我」没有改口收萝卜。护栏这次又收紧了一轮（买酒、为什么帮我），**须再部署**云端才与本地完全一致。

---

### 2026-08-21 | 闲聊当令 / 动作旁白 / 第八天睡不着

实机：①聊英雄联盟回「对啊」变成「要买几包 / 我这就去种」；②嘴里出现「我抖了下尾巴」；③第八天夜里睡不着。

1. 闲聊短句不再被 LLM 的 plant/open_shop 覆盖。上一句不是农事邀约时清掉残留 pending。双端提示词同步。
2. 说话去掉自己给自己写的动作旁白（抖尾巴、竖耳朵等）。
3. D8 本子夜里仍可邀请；聊天睡觉会强制打开信纸，不再只提示「再陪陪小狸」。

**Cloud Run 须再部署。**

---
